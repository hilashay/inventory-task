import styled from "styled-components";

const NoteForLastUnits = () => {
  return <Note>last units!</Note>;
};

export default NoteForLastUnits;

const Note = styled.div`
  align-self: center;
  margin-left: 50%;
  font-weight: bold;
  color: #f2adbc; ;
`;
